# =============================================================================
# MagicGuard Environment Configuration
# =============================================================================
# This file contains all environment variables that can be used to configure
# MagicGuard for different environments (development, testing, production).
#
# Copy this file to .env and adjust values for your environment:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Path to the SQLite signature database file
# Default: ~/.magicguard/data/signatures.db
# Docker: /data/signatures.db
MAGICGUARD_DB_PATH=

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# Directory where log files will be stored
# Default: ~/.magicguard/log
# Docker: /logs
MAGICGUARD_LOG_DIR=

# Log level for application output
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: DEBUG (development), INFO (production)
MAGICGUARD_LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# File Size Limit
# -----------------------------------------------------------------------------
# Maximum file size to process (in bytes)
# Default: 104857600 (100MB)
# Examples: 52428800 (50MB), 209715200 (200MB), 1073741824 (1GB)
MAGICGUARD_MAX_FILE_SIZE=

# -----------------------------------------------------------------------------
# Data Directory
# -----------------------------------------------------------------------------
# Directory for application data (database, cache, etc.)
# Default: ~/.magicguard/data
MAGICGUARD_DATA_DIR=

# -----------------------------------------------------------------------------
# Docker-Specific Configuration
# -----------------------------------------------------------------------------
# These variables are used when running MagicGuard in Docker containers
# Uncomment and configure when using Docker/docker-compose

# Directory containing files to scan (mounted as /scan in container)
# SCAN_DIR=./scan

# Directory for log output (mounted as /logs in container)
# LOG_DIR=./logs

# Log level for Docker container
# LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Application Limits
# -----------------------------------------------------------------------------
# MAX_FILE_SIZE: Configurable via MAGICGUARD_MAX_FILE_SIZE (see above)
#
# The following are configured in src/magicguard/utils/config.py and cannot be
# overridden via environment variables. Listed here for reference:
#
# MAX_SIGNATURE_LENGTH=64          # 64 bytes maximum signature length
# MAX_LOG_FILES=30                 # Keep 30 days of log files

# -----------------------------------------------------------------------------
# Development Configuration
# -----------------------------------------------------------------------------
# Uncomment for local development with custom paths

# Development database path
# MAGICGUARD_DB_PATH=/tmp/magicguard/signatures.db

# Development log directory
# MAGICGUARD_LOG_DIR=/tmp/magicguard/logs

# Development data directory
# MAGICGUARD_DATA_DIR=/tmp/magicguard/data

# Verbose logging for debugging
# MAGICGUARD_LOG_LEVEL=DEBUG

# -----------------------------------------------------------------------------
# Testing Configuration
# -----------------------------------------------------------------------------
# Uncomment for testing environment with isolated paths

# Test database (ephemeral)
# MAGICGUARD_DB_PATH=/tmp/test-magicguard/signatures.db

# Test log directory
# MAGICGUARD_LOG_DIR=/tmp/test-magicguard/logs

# Test data directory
# MAGICGUARD_DATA_DIR=/tmp/test-magicguard/data

# Test log level
# MAGICGUARD_LOG_LEVEL=DEBUG

# -----------------------------------------------------------------------------
# Production Configuration Examples
# -----------------------------------------------------------------------------
# Uncomment and adjust for production deployment

# Production database (persistent storage)
# MAGICGUARD_DB_PATH=/var/lib/magicguard/signatures.db

# Production logs (system log directory)
# MAGICGUARD_LOG_DIR=/var/log/magicguard

# Production data directory
# MAGICGUARD_DATA_DIR=/var/lib/magicguard/data

# Production log level (less verbose)
# MAGICGUARD_LOG_LEVEL=WARNING

# -----------------------------------------------------------------------------
# CI/CD Configuration (Jenkins, GitHub Actions, etc.)
# -----------------------------------------------------------------------------
# Uncomment for continuous integration environments

# CI database path
# MAGICGUARD_DB_PATH=/tmp/ci-magicguard/signatures.db

# CI log directory
# MAGICGUARD_LOG_DIR=/tmp/ci-magicguard/logs

# CI data directory
# MAGICGUARD_DATA_DIR=/tmp/ci-magicguard/data

# CI log level
# MAGICGUARD_LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Security Notes
# -----------------------------------------------------------------------------
# 1. Never commit the .env file to version control
# 2. Ensure proper file permissions: chmod 600 .env
# 3. Use absolute paths for production deployments
# 4. Validate paths before using in production
# 5. Keep sensitive data out of environment variables when possible

# -----------------------------------------------------------------------------
# Usage Examples
# -----------------------------------------------------------------------------
# Local development:
#   export MAGICGUARD_LOG_LEVEL=DEBUG
#   magicguard scan document.pdf
#
# Docker Compose:
#   LOG_LEVEL=INFO docker-compose run scanner scan /scan/file.pdf
#
# Python dotenv:
#   from dotenv import load_dotenv
#   load_dotenv()
#   # Environment variables are now available
